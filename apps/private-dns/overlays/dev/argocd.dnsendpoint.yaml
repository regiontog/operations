apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  endpoints:
    - dnsName: argocd.dev.internal
      recordTTL: 60
      recordType: A
      targets:
        - 10.96.80.80

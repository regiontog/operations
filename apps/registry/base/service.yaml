apiVersion: v1
kind: Service
metadata:
  name: registry
  namespace: cluster-manager
spec:
  selector:
    app.kubernetes.io/name: registry
  ports:
    - name: registry
      port: 5000
      targetPort: registry
